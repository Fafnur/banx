import { MainMenuLinkActivePipe } from './main-menu-link-active.pipe';

describe('MenuLinkActivePipe', () => {
  it('create an instance', () => {
    const pipe = new MainMenuLinkActivePipe();
    expect(pipe).toBeTruthy();
  });
});
