import { ButtonSmallDirective } from './button-small.directive';

describe('ButtonSmallDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSmallDirective();
    expect(directive).toBeTruthy();
  });
});
