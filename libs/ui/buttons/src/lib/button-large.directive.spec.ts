import { ButtonLargeDirective } from './button-large.directive';

describe('ButtonLargeDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonLargeDirective();
    expect(directive).toBeTruthy();
  });
});
