import { ButtonMediumDirective } from './button-medium.directive';

describe('ButtonMediumDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonMediumDirective();
    expect(directive).toBeTruthy();
  });
});
